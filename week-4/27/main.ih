#include "../26/person.ih"

using namespace std;

void readData(Person array[], size_t size, std::istream &inStream);
void writeData(Person array[], size_t size, std::ostream &outStream);
istream iStream(streambuf *sb);